apiVersion: v1
kind: Service
metadata:
  name: my-python-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: my-python-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}